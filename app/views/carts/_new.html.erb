<%= form_with url: carts_path do |f| %>
    <%= f.hidden_field :product_id, value: product.id %>
    <%= f.button 'اضافة للسلة', type: 'submit', class: 'btn btn-primary btn-sm shadow-none' %>
<% end %>