<h2>
 منتجاتنا
</h2>

<% @products.each do |product| %>
  <div>
    <%= product.name %>
    <%= product.description %>
    <%= form_with url: carts_path do |f| %>
      <%= f.hidden_field :product_id, value: product.id %>
      <%= f.submit 'اضافة للسلة' %>
    <% end %>
  </div>
<% end %>